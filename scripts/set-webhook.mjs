import { telegram } from './tg-api.mjs';const {WEBHOOK_PUBLIC_URL:url,WEBHOOK_PATH='/telegram-webhook',WEBHOOK_SECRET:secret_token}=process.env;if(!url||!secret_token)throw new Error('WEBHOOK_PUBLIC_URL and WEBHOOK_SECRET are required');await telegram('setWebhook',{url:`${url.replace(/\/$/,'')}${WEBHOOK_PATH}`,secret_token,drop_pending_updates:false});console.log('Webhook registered (secrets not displayed).');
