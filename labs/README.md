# Fault labs
Install/start `tg-bot-lab.service`; run each directory's `break.sh`, collect evidence with `observe.sh`, then `fix.sh` and `verify.sh`. Scripts require root, modify only lab unit/environment, retain backups, and are repeatable. Never use them against `tg-bot.service`.
